## Models

| File | Used for |
|---|---|
| `dufflebag.obj` | Hero + Craftsmanship scroll animation |
| `stamp.obj` + `stamp.mtl` | Lifestyle stamp animation (persists after) |

Config lives in `src/components/ProductViewer/DuffelBagScene.tsx` → `MODEL_CONFIGS`.

Adjust `targetSize`, `scale`, and `position` per variant if sizing looks off.
