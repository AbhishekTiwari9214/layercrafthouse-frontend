/** Shared logo dimensions — fixed overlay + footer outro must match exactly */
export const LOGO_BOX_CLASS =
  "aspect-square h-[min(90vw,480px)] w-[min(90vw,480px)] md:h-[720px] md:w-[720px]";

export const LOGO_IMAGE_SIZES = "(max-width: 768px) 90vw, 720px";
